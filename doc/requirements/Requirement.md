API Design
1 check all publsihed status baselines(even it's applying for deleted/retired requirement)
2 show timeline of requirement changes
3 show current status
4 approval workflow

System Design
1 Timeline view for the baselines 
2 Baseline list view by status (Published baselines should still display as Published even if a retire/delete action is pending)
3 Approval workflow

UI Design
1 Timeline view for current step
2 - Baseline table view for different status baselines
- Published baselines should still display as Published even if retire/delete is pending
  3 Approval workflow